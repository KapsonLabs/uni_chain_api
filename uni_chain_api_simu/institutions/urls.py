# from django.urls import path, include
# from .views import CertificateUpload, CertificateVerificationRequestView

# urlpatterns = [
#     path('certificates/upload/', CertificateUpload.as_view(), name="certificate-upload"),
#     path('certificates/request_verification/', CertificateVerificationRequestView.as_view(), name="certificate-request"),
# ]